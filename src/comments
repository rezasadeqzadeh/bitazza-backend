— diffPrice method:
by default call by reference e inja, kheyli mafhoom nist argument change beshe va return beshe.
classic for loop kheyli recommended nist.

- file
  socket-client.service.ts
  —
  fetchInstruments
  behtare e Promise return kone ta callback.
  — line 134,
  o.map((item) => {
  in jaleb nist. map khodesh return mikone, chera variable joda toosh use kardin bara push?

— top method:
mishe 2 ta kar anjam dad, (1) sort va slice sare ham bashe:
x.sort((a,b) => a > b).slice(0, 2)

---

Front

- file
  Login.tsx
  behtare logic e api call too action bashe, component faghat masoole UI e.

- file e
  TopGainers.tsx

Auth component joda beshe.
line 29 dateFormat mitoone biiron bashe.
hess mikonam file format nashode ba editor!
vase kar ba datetime behtare e library use konid. dayjs (behtare) ya momentjs.

- file InstrumentsActions
  https://github.com/rezasadeqzadeh/bitazza-front/blob/main/src/redux/actions/InstrumentActions.ts#L13
  az env path e server o begire behtare.

- file e index.tsx
  behtare routing bere too App.tsx.

yekam type system e ts use beshe khoobe. masalan bara har state variable.
behtare compoenent e Bootstrap use beshe ta bishtar classname.

dar kol har 2 proje ; niaz nist, config mishe ba prettier ke asan nabashe.
